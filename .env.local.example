# No environment variables required to run locally.
# The Next.js API route calls Seek's GraphQL endpoint directly (no auth needed).

# Optional: if you later add a Cloudflare Worker as a proxy for production,
# set this to your worker URL and update app/api/lookup/route.ts to use it.
# SEEK_PROXY_URL=https://your-worker.workers.dev
